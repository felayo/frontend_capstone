import { Typography } from "@mui/material";

function GuestsErrorMessage() {
  return <Typography color="red" pl={10}>* Enter a number of guests from 1 to 10</Typography>;
}

export default GuestsErrorMessage;
